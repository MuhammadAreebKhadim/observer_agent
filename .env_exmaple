GROQ_API_KEY = 'your_groq_api_key_here'
USE_MOCK_SUMMARY = False # or True (if True, will use mock summary data for logs instead of calling the real API)
SCREENSHOT_WAIT_TIME = 0 # in seconds before taking a screenshot
CAPTURE_MODE = recording # or screenshot (recording is default)
AUTO_LOOP_DELAY = 5 # in seconds between each loop of the auto.py script